DECODE can be installed following the instructions on https://github.com/TuragaLab/DECODE.

A computer equipped with a NVIDIA GPU is needed to train a DECODE model.
The GPU used in our lab is the GeForce RTX 2060 12GB

DECODE training runtimes are several hours and DECODE fitting runtimes are 10 to 40 minutes depending on image size.
